network:
  enable_ipv6: true

ota:
  - platform: esphome
    id: ota_esphome
    password: !secret ota_password

wifi:
  id: wifi_id
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: "${name} Fallback"
  on_connect: 
    - lambda: id(voice_assistant_phase) = ${voice_assist_noHA_phase_id};

captive_portal: